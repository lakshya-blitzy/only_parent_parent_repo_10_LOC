# SHA-256 of the 15 bytes `node index.js` writes to stdout with no arguments ("12\n" five times, exit 0).
# Produced and verified with Node.js 22.23.1, the runtime pinned in .nvmrc.
# Verify with: node index.js | sha256sum
# The recorded value is the backward-compatibility contract: on a mismatch fix index.js, never this hash.
56990d15cb2da6455eec9f9d56432fdeb611e39388f4764725129962343e5fc4  legacy-index-js-stdout
