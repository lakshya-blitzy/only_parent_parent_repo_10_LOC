[project]
name = "only_parent_parent_repo_10_loc"
version = "1.1.0"
description = "Multi-language sample repository exposing a uniform /health endpoint."
requires-python = ">=3.14"

# Standard library only, by design; requires-python is a floor, the exact pin lives in .python-version.
dependencies = []
